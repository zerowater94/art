<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.art.app.base.org.dao.AuthGrantDao">
 	<sql id="columns">
 		a.auth_grant_id, a.auth_assign_id, a.auth_grant_type, 
 		a.description,
   		a.reg_user_id, a.reg_dtm, a.upd_user_id, a.upd_dtm
 	</sql>
    <select id="selectList" resultType="com.art.app.base.org.domain.AuthGrantVO">
        SELECT  <include refid="columns" /> 
		FROM    tco_auth_grant a
        WHERE   1 = 1
        AND     a.auth_grant_id = #{authGrantId}
		ORDER BY a.auth_grant_id
    </select>
    
    <select id="selectData" resultType="com.art.app.base.org.domain.AuthGrantVO">
        SELECT  <include refid="columns" />
		FROM    tco_auth_grant a
        WHERE   1 = 1
        AND     a.auth_grant_id = #{authGrantId}
    </select>
    
    <insert id="insert" parameterType="com.art.app.base.org.domain.AuthGrantVO">
        INSERT INTO tco_auth_grant (
        	auth_grant_id, auth_assign_id, auth_grant_type, 
 			description,
	   		reg_user_id, reg_dtm, upd_user_id, upd_dtm 
        )VALUES(
        	#{authGrantId}, #{authAssignId}, #{authGrantType}
        	#{description},
        	#{currUserId}, #{currDateTime},#{currUserId}, #{currDateTime}
        )
    </insert>
    
    <update id="update" parameterType="com.art.app.base.org.domain.AuthGrantVO">
        UPDATE tco_auth_grant 
        SET    auth_assign_id = #{authAssignId},
        	   auth_grant_type = #{authGrantType},
        	   description  = #{description},
        	   upd_user_id  = #{currUserId},
        	   upd_dtm      = #{currDateTime}
        WHERE  auth_grant_id = #{authGrantId}
    </update>
    
    <delete id="delete" parameterType="com.art.app.base.org.domain.AuthGrantVO">
        DELETE FROM tco_auth_grant
        WHERE  auth_grant_id = #{authGrantId}
    </delete>
    
</mapper> 
